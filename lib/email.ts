import { Resend } from "resend";
import { requireEmailEnv } from "@/lib/env";

export async function sendCapsuleReadyEmail(to: string, title: string, id: string) { const env = requireEmailEnv(); const resend = new Resend(env.RESEND_API_KEY); return resend.emails.send({ from: `${env.RESEND_FROM_NAME} <${env.RESEND_FROM_EMAIL}>`, to, subject: "Your Capsule is ready", html: `<div style="background:#090c14;color:#e9edf5;padding:40px;font-family:Arial,sans-serif"><div style="max-width:520px;margin:auto"><p style="color:#b8a4ff;letter-spacing:2px;font-size:11px">CAPSULE BY FIRSTCOMMIT</p><h1 style="font-size:32px">Your Capsule is ready.</h1><p style="color:#aeb7c8;line-height:1.7">You left something for yourself. <strong>${escapeHtml(title)}</strong> is ready to open.</p><a href="${env.NEXT_PUBLIC_APP_URL}/dashboard/${id}" style="display:inline-block;background:#b8a4ff;color:#151326;padding:14px 20px;border-radius:10px;text-decoration:none;font-weight:bold">Open Capsule ↗</a></div></div>` }); }
function escapeHtml(value: string) { return value.replace(/[&<>'"]/g, (char) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", "'": "&#39;", '"': "&quot;" })[char] || char); }
