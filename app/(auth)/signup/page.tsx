import Link from "next/link";
import { signUp } from "../actions";

export default async function Signup({ searchParams }: { searchParams: Promise<{ error?: string }> }) { const params = await searchParams; return <main className="auth-wrap"><section className="auth-card glass"><Link className="brand" href="/"><span className="brand-mark">✦</span> capsule</Link><h1>Keep a thought.</h1><p>Make a small promise to your future self.</p>{params.error && <p className="form-error">{params.error}</p>}<form action={signUp}><div className="field"><label htmlFor="displayName">Your name <span className="subtle">(optional)</span></label><input id="displayName" name="displayName" autoComplete="name" /></div><div className="field"><label htmlFor="email">Email</label><input id="email" name="email" type="email" required autoComplete="email" /></div><div className="field"><label htmlFor="password">Password</label><input id="password" name="password" type="password" minLength={8} required autoComplete="new-password" /></div><button className="button full" type="submit">Begin <span>↗</span></button></form><div className="auth-footer">Already have an account? <Link href="/login">Sign in</Link></div></section></main> }
