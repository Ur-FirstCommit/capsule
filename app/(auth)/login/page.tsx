import Link from "next/link";
import { signIn } from "../actions";

 export default async function Login({ searchParams }: { searchParams: Promise<{ error?: string; message?: string }> }) { const params = await searchParams; return <main className="auth-wrap"><section className="auth-card glass"><Link className="brand" href="/"><span className="brand-mark">✦</span> capsule</Link><h1>Welcome back.</h1><p>Your future self has been waiting.</p>{params.error && <p className="form-error">{params.error}</p>}{params.message && <p className="eyebrow">{params.message}</p>}<form action={signIn}><div className="field"><label htmlFor="email">Email</label><input id="email" name="email" type="email" required autoComplete="email" /></div><div className="field"><label htmlFor="password">Password</label><input id="password" name="password" type="password" required autoComplete="current-password" /></div><button className="button full" type="submit">Sign in <span>↗</span></button></form><div className="auth-footer"><Link href="/forgot-password">Forgot password?</Link><br />New here? <Link href="/signup">Create an account</Link></div></section></main> }
